/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/fcca-sports',
  assetPrefix: '/fcca-sports/',
  images: { unoptimized: true },
};

module.exports = nextConfig;
